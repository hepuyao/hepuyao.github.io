# 桌面透明笔记本

独立悬浮窗：鼠标悬停清晰可编辑，离开缩成可贴边小条。不依赖网页同步。

```bash
cd Jasor/2608_notebook_tran_desktop
pip3 install -r requirements.txt
python3 app.py
```

数据：`~/.culinux/notebook_transparent.json`

网页 + 本机助手见 `../2608_notebook_vague/`。
